[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gradiend"
version = "0.2.0"
description = "GRADIEND: Gradient-based Targeted Feature Learning within Neural Networks"
readme = "PYPI.md"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}
authors = [
    {name = "Jonathan Drechsel", email = "jonathan.drechsel@uni-passau.de"}
]
keywords = ["machine learning", "neural networks", "feature learning", "interpretability"]
classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
]

dependencies = [
    "torch>=2.0.0",
    "transformers>=4.30.0,<6.0.0",
    "numpy>=1.20.0",
    "pandas>=1.3.0",
    "tqdm>=4.60.0",
]

[project.optional-dependencies]
recommended = [
    "accelerate>=0.26.0",
    "safetensors>=0.3.0",
    "sentencepiece>=0.1.99",

    "datasets>=3.0.2",
    "spacy>=3.5.0,<3.8.0; python_version < '3.10'",
    "spacy>=3.5.0; python_version >= '3.10'",
    "pyarrow>=14.0.0",
    "pyahocorasick>=2.0.0",

    "matplotlib>=3.5.0",
    "seaborn>=0.12.0",
    "plotly>=5.0.0",
    "venn>=0.1.0",
    "matplotlib_venn>=0.11.7",
    "adjustText>=0.7.3",
]
data = [
    "datasets>=3.0.2",
    "spacy>=3.5.0,<3.8.0; python_version < '3.10'",
    "spacy>=3.5.0; python_version >= '3.10'",
    "pyarrow>=14.0.0",
    "pyahocorasick>=2.0.0",
]
plot = [
    "matplotlib>=3.5.0",
    "seaborn>=0.12.0",
    "plotly>=5.0.0",
    "venn>=0.1.0",
    "matplotlib_venn>=0.11.7",
    "adjustText>=0.7.3",
]
dev = [
    "mkdocs-material>=9.0.0",
    "mkdocstrings[python]>=0.24.0",
    "pre-commit>=3.0.0",
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]

[project.urls]
Homepage = "https://github.com/aieng-lab/gradiend"
Documentation = "https://aieng-lab.github.io/gradiend/"
Repository = "https://github.com/aieng-lab/gradiend"
"Bug Tracker" = "https://github.com/aieng-lab/gradiend/issues"

[tool.pytest.ini_options]
pythonpath = "."
basetemp = ".pytest_tmp_local"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests (run actual training)",
]

[tool.coverage.run]
source = ["gradiend"]
omit = ["*/tests/*", "*/examples/*", "*/experiments/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["gradiend*"]
exclude = ["gradiend.examples*"]

[tool.setuptools.package-data]
gradiend = ["py.typed"]

[tool.ruff.lint.per-file-ignores]
# Star-import / re-export modules: imports look unused locally but are part of the public namespace.
"gradiend/trainer/suite/definitions.py" = ["F401"]
"gradiend/util/__init__.py" = ["F401"]
