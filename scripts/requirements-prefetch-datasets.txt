# Minimal env for scripts/prefetch_hf_datasets.py
#
# datasets 3.x removed HuggingFace loading scripts (genter.py, etc.).
# Use datasets 2.x here once to warm the HF cache and/or export local Parquet.
#
#   conda create -n gradiend-prefetch python=3.11 -y
#   conda activate gradiend-prefetch
#   pip install -r scripts/requirements-prefetch-datasets.txt

datasets>=2.18.0,<3.0.0
huggingface_hub>=0.23.0
certifi>=2024.2.2
pyarrow>=14.0.0
pandas>=1.3.0
tqdm>=4.60.0
